@model Chill_Closet.Models.Product

@{
    ViewData["Title"] = "Thêm Sản phẩm mới";
}

<div class="admin-dashboard spad">
    <div class="container">
        <div class="row">
            <div class="col-lg-3">
                <div class="admin-sidebar">
                    <h4>Admin Menu</h4>
                    <ul class="admin-sidebar-nav">
                        <li><a asp-controller="Admin" asp-action="Index"><i class="fa fa-tachometer"></i> Dashboard</a></li>
                        <li class="active"><a asp-controller="Admin" asp-action="Products"><i class="fa fa-tags"></i> Quản lý Sản phẩm</a></li>
                        <li><a href="#"><i class="fa fa-th-large"></i> Quản lý Danh mục</a></li>
                        <li><a href="#"><i class="fa fa-shopping-cart"></i> Quản lý Đơn hàng</a></li>
                        <li><a href="#"><i class="fa fa-users"></i> Quản lý Người dùng</a></li>
                        <li><a href="#"><i class="fa fa-newspaper-o"></i> Quản lý Blog</a></li>
                    </ul>
                </div>
            </div>
            <div class="col-lg-9">
                <div class="admin-content">
                    <div class="admin-content-title">
                        <h2>Thêm Sản phẩm mới</h2>
                    </div>

                    <div class="admin-form-container">
                        <form asp-action="Create" method="post" enctype="multipart/form-data">
                            <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

                            <div class="form-group">
                                <label asp-for="Name" class="form-label">Tên Sản phẩm</label>
                                <input asp-for="Name" class="form-control" />
                                <span asp-validation-for="Name" class="text-danger"></span>
                            </div>

                            <div class="form-group">
                                <label asp-for="Price" class="form-label">Giá</label>
                                <input asp-for="Price" class="form-control" />
                                <span asp-validation-for="Price" class="text-danger"></span>
                            </div>

                            <div class="form-group">
                                <label asp-for="CategoryId" class="form-label">Danh mục</label>
                                <select asp-for="CategoryId" class="form-control" asp-items="ViewBag.Categories">
                                    <option value="">-- Chọn danh mục --</option>
                                </select>
                                <span asp-validation-for="CategoryId" class="text-danger"></span>
                            </div>

                            <div class="form-group">
                                <label asp-for="Description" class="form-label">Mô tả</label>
                                <textarea asp-for="Description" class="form-control" rows="4"></textarea>
                                <span asp-validation-for="Description" class="text-danger"></span>
                            </div>

                            <div class="form-group">
                                <label class="form-label">Hình ảnh sản phẩm (có thể chọn nhiều ảnh)</label>
                                <input type="file" name="imageFiles" class="form-control" multiple />
                            </div>

                            <div class="form-buttons mt-4">
                                <button type="submit" class="btn btn-outline-secondary">Tạo Mới</button>
                                <a asp-action="Products" class="btn btn-outline-secondary ml-2">Quay lại</a>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}